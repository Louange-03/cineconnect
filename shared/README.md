## shared

Ce dossier contient le code partagé entre le frontend et le backend (types, constantes, helpers).

Objectif : éviter la duplication de types (ex: `Film`, `User`, `AuthResponse`) et garder le typage cohérent.

